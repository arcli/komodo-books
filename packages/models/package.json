{
  "name": "models",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "pnpm -F models exec tsc"
  },
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  },
  "devDependencies": {
    "@types/node": "^20.8.9",
    "prisma": "^5.5.0"
  },
  "dependencies": {
    "@prisma/client": "^5.5.0"
  }
}
