{
  "name": "models",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "pnpm -F models exec tsc"
  },
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  },
  "devDependencies": {
    "prisma": "^5.4.2"
  },
  "dependencies": {
    "@prisma/client": "5.4.2",
    "prisma-class-generator": "^0.2.8"
  }
}
